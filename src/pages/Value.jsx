import React from 'react';
import ValueBlock from '../components/ValueBlock';

const Value = () => {
    return (
        <div>
            <ValueBlock/>
        </div>
    );
};

export default Value;